#include <iostream>

using namespace std;

int main()
{

    // int a = 10;
    // int *p = &a;

    // *p = 100;

    // cout << "p = " << *p << endl;
    // cout << "a = " << a << endl;

    // cout << "A is " << a << endl;
    // cout << "Address of A is " << &a << endl;
    // cout << "Address stored in p is " << p << endl;
    // cout << "Value pointed to by a is " << *p << endl;
    // cout << "Address Of P " << &p << endl;

    // int a[] = {10,20,30,40,50};

    // int *p = a;
    // p++;

    // cout << "p = " << *p << endl;

    return 0;
}